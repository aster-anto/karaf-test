machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t circleci/karaf-test .

test:
  override:
    - docker run -d -p 8181:8181 circleci/karaf-test; sleep 30
    - curl --retry 10 --retry-delay 5 -v http://localhost:8181
